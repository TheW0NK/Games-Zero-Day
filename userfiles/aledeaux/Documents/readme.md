# Aegis OS

This is the hosted build. Everything in here is a real file on disk under `./userfiles` on the server — edit it in Files, in the Terminal, or directly in your editor, and it shows up in both places.
